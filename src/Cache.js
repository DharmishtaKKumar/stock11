import React from "react";
class Cache {
  isShowMain = false;

  getIsSHow(key) {
    // get the value out for the given key
    return isShowMain;
  }

  setIsShow(key) {
    // get the value out for the given key
    isShowMain = key;
  }
}

module.exports = new Cache();
